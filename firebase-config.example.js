/* =========================================================
   SyncWave — configuration
   Fill these in with your own Firebase + Cloudinary values.
   This file is safe to be public — it only contains public
   client identifiers, never secret keys.
   ========================================================= */

// Firebase Console → Project settings → General → Your apps → SDK setup
// const firebaseConfig = {
//   apiKey: "YOUR_FIREBASE_API_KEY",
//   authDomain: "YOUR_PROJECT_ID.firebaseapp.com",
//   databaseURL: "https://YOUR_PROJECT_ID-default-rtdb.firebaseio.com",
//   projectId: "YOUR_PROJECT_ID",
//   storageBucket: "YOUR_PROJECT_ID.appspot.com",
//   messagingSenderId: "YOUR_SENDER_ID",
//   appId: "YOUR_APP_ID"
// };


// Cloudinary Console → Settings → Upload → Upload presets
// The preset MUST be "Unsigned" — never put your API Secret here or anywhere in client code.
const CLOUDINARY_CONFIG = {
  cloudName: "",
  uploadPreset: ""
};
